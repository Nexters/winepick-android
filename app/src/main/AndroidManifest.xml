<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="kr.co.nexters.winepick">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--queries에 카카오톡 패키지 추가-->
    <queries>
        <package android:name="com.kakao.talk" />
    </queries>

    <application
        android:name=".WinePickApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity android:name=".ui.home.HomeActivity"></activity>

        <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="@string/kakao_app_key" />

        <activity android:name=".ui.login.LoginActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- survey -->
        <activity android:name=".ui.survey.SurveyActivity" />
        <activity android:name=".ui.survey.SurveyResultActivity">
            <intent-filter>
                <action android:name="android:intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- "kakao{YOUR_NATIVE_APP_KEY}://kakaolink" 형식의 앱 실행 스킴을 설정하는데 사용 -->
                <data
                    android:scheme="@string/kakao_scheme"
                    android:host="@string/kakaolink_host" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.survey.ScreenSlidePagerActivity"/>

        <activity android:name=".example.java.normal.JavaDefaultActivity" />
        <activity android:name=".example.kotlin.normal.KotlinDefaultActivity" />
        <activity android:name=".example.java.viewmodel.JavaViewModelActivity" />
        <activity android:name=".example.kotlin.viewmodel.KotlinViewModelActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".ui.search.SearchActivity"
            android:theme="@style/AppNoActionBar">
        </activity>
        <activity
            android:name=".ui.search.SearchFilterActivity"
            android:theme="@style/SearchFilterNoActionBar" />

    </application>

</manifest>
